package tmmscode.forumHub.domain.course;

// You need to change this enum for any new category that you want to add
// If you want to rename the categories, a new migration is necessary to update de database old names
public enum CourseCategory {
    PROGRAMING,
    SOFT_SKILL
}
