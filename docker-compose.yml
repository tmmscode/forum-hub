version: '3.8'

services:
  forumhub:
    build: .
    ports:
      - "8080:8080"
    environment:
      DB_HOST: localhost
      DB_NAME: tmmscode_forumhub
      DB_USERNAME: root
      DB_PASSWORD: mysql123
      JWT_SECRET: lisa
